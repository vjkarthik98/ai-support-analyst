[pytest]
# Put the repo root on sys.path so tests can `import app`.
# Without this, pytest only adds tests/ to the path and every import fails.
pythonpath = .
testpaths = tests
# Show the reason for every non-passing result, and keep output terse.
addopts = -q -ra
